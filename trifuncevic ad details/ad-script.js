/*
const infoParts = document.querySelectorAll(".icon-info");
const adWrapper = document.querySelector(".ad-info-wrapper");
const infoSec = document.querySelector(".info-part");
const displayRows = document.querySelector(".display-rows");

console.log(infoParts);

if (infoParts.length > 8) {
  const half = infoParts.length / 2;
  // const firstHalf = infoParts.slice(0, 3);
  // console.log(firstHalf);

  // Create an empty array to hold the selected elements
  const selectedElements = [];

  // Loop through the original NodeList and select every other element
  for (let i = 0; i < infoParts.length; i += half) {
    selectedElements.push(infoParts.item(i));
  }

  // Create a new NodeList from the selected elements array

  // const selectedList = document.createNodeList(...selectedElements);
  console.log(selectedElements);
}
*/
